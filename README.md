# Twitter GenStage
## Description
Stream twitter streams via Phoenix channels

### Run it
`mix deps.get`

`cd apps/web`

`npm install`

`cd ../..`

`mix phoenix.server`

navigate to [http://localhost:4000](http://localhost:4000)

